# Hyperlift — demo installers

Public downloads for the Hyperlift demo (macOS `.dmg`, Windows `.exe`).
The demo is the full plugin — unlicensed it periodically adds a short noise burst.

Latest: use the stable links
- `releases/latest/download/Hyperlift-mac.dmg`
- `releases/latest/download/Hyperlift-Win64.exe`
